#include <stdio.h>
 
int main ()
{
   int y;
   int a = 1;
   int b = 2;
   int c = 3;
  {
    do
   
       printf("Do you want to order Coffe ? (yes la y / no la so khac): \n");
       scanf("%d", &y);
	{
	do
	printf("menu: capuchino(a) capuchino caramel(b) espresso(c)");
    	scanf("%d", &a, &b, &c );
    while(a==a, b==b, c==c);
    printf (" ok \n");
}
   while(y==y);
   printf("Good Bye\n");
}
   return 0;
}

